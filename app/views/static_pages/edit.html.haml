%h2
  Editing &ldquo;#{h @static_page.title}&rdquo;
= form_for @static_page, :html => { :method => :put } do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Save these changes"
