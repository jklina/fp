%h2 Submissions to Feature
= render :partial => @submissions
%hr.invisible/
%h3 Submit Feature
- form_for @feature, :url => featured_index_path, :html => { :multipart => true } do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Feature these submissions"
