%h2
  Editing &ldquo;#{h @announcement.title}&rdquo;
- form_for @announcement, :html => { :method => :put } do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Save these changes"
